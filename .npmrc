# Fail instead of warn when the Node or npm version does not match the
# engines field of package.json. Older npm versions resolve optional peer
# dependencies differently and produce a package-lock.json that npm ci then
# rejects on other machines.
engine-strict=true
